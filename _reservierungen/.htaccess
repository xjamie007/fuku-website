# Dieses Verzeichnis enthält personenbezogene Daten (Reservierungen).
# Zugriff über das Web ist vollständig gesperrt – nur per FTP/SSH lesen.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
